<UserControl x:Class="GPUControl.Controls.OcEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GPUControl.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <UserControl.DataContext>
        <local:OcEditorViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="51"/>
                <RowDefinition Height="59"/>
                <RowDefinition Height="57"/>
                <RowDefinition Height="44"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <!-- Power target -->
            <TextBlock Grid.Row="0" HorizontalAlignment="Center" Margin="0,7,0,0" TextWrapping="Wrap" Text="Power" VerticalAlignment="Top" Width="80" Height="16"/>
            <TextBlock Grid.Row="0" HorizontalAlignment="Center" Margin="0,28,0,0" TextWrapping="Wrap" Text="{Binding PowerTarget}" VerticalAlignment="Top" Height="16" Width="80"/>
            <Slider Grid.Column="1" Margin="10,0,10,0" VerticalAlignment="Center" Height="18" Value="{Binding PowerTarget, Mode=TwoWay}" Minimum="{Binding PowerTargetRange.Min}" Maximum="{Binding PowerTargetRange.Max}" IsSnapToTickEnabled="True"/>


            <!-- Core clock -->
            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="0,7,0,0"  TextWrapping="Wrap" Text="Core Offset" VerticalAlignment="Top" Width="80" Height="16"/>
            <TextBlock Grid.Row="1" HorizontalAlignment="Center" Margin="0,28,0,0" TextWrapping="Wrap" Text="{Binding GpuClock}" VerticalAlignment="Top" Height="16" Width="80"/>
            <Slider Grid.Column="1" Grid.Row="1" Margin="10,0,10,0" VerticalAlignment="Center" Height="18" Value="{Binding GpuClock, Mode=TwoWay}" Minimum="{Binding GpuClockRange.Min}" Maximum="{Binding GpuClockRange.Max}" IsSnapToTickEnabled="True"/>

            <!-- Memory clock -->
            <TextBlock Grid.Row="2" HorizontalAlignment="Center" Margin="0,7,0,0"  TextWrapping="Wrap" Text="Memory Offset" VerticalAlignment="Top" Width="80" Height="16"/>
            <TextBlock Grid.Row="2" HorizontalAlignment="Center" Margin="0,28,0,0" TextWrapping="Wrap" Text="{Binding MemoryClock}" VerticalAlignment="Top" Height="16" Width="80"/>
            <Slider Grid.Column="1" Grid.Row="2" Margin="10,0,10,0" VerticalAlignment="Center" Height="18" Value="{Binding MemoryClock, Mode=TwoWay}" Minimum="{Binding MemoryClockRange.Min}" Maximum="{Binding MemoryClockRange.Max}" IsSnapToTickEnabled="True"/>

        </Grid>
        <Button Content="Apply" HorizontalAlignment="Left" Margin="10,10,10,10" Grid.Row="1" Height="20" VerticalAlignment="Bottom" Click="Button_Click"/>
    </Grid>
</UserControl>
